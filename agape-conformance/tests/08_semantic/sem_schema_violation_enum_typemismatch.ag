//! id:        sem_schema_violation_enum_typemismatch
//! section:   08_semantic
//! expect:    accept
//! contains:  TypeMismatch
//! provider:  schema_violation
//! spec:      §8 (Enum compiles to a closed enum schema; schema failure raises TypeMismatch)
//! ---
enum Ticket { Billing, Bug }
agent A {
  on awake {
    Ticket ticket = self <- "classify ticket";
  }
}
spawn A a; awake a;

//! id:        gov_emit_ungranted_reject
//! section:   13_governance
//! expect:    reject
//! error:     AuthorityViolation
//! spec:      §13 (an agent may only emit event types in its grants)
//! ---
event Note(text msg);
agent A {
  on awake { emit Note("hi"); }
}
spawn A a; awake a;

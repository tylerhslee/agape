//! id:        repro_decide_off_spine
//! section:   15_reproducibility
//! expect:    accept
//! spec:      §15 (decide is a pure projection of a Credence already on the spine; off-spine itself)
//! ---
sync bool gate(Credence<bool> c) { return decide(c, > 0.8); }

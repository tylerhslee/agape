//! id:        fn_sync_decide_by_principal_reject
//! section:   04_functions
//! expect:    reject
//! error:     ColorViolation
//! spec:      §4, §13 (`p decide c by R` for a principal reaches the identity dependency → async; a sync fn may not)
//! ---
principal alice;
sync null ok(Credence<bool> c) { alice decide c by confidence 0.9; return null; }

//! id:        fn_sync_store_reject
//! section:   04_functions
//! expect:    reject
//! error:     ColorViolation
//! spec:      §9, §10 (store reaches the provider-backed memory substrate, so a sync function may not call it)
//! ---
sync null cache() {
  store("fact note");
  return null;
}

//! id:        fn_sync_verify_by_principal_reject
//! section:   04_functions
//! expect:    reject
//! error:     ColorViolation
//! spec:      §4, §13 (verify … by Principal reaches the identity seam → async)
//! ---
principal alice;
sync null ok(text memo) { verify memo by alice; return null; }

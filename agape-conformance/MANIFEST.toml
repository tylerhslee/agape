# Agape v1.0.0-beta.2026.8.6.0 conformance manifest (generated by build_manifests.py from the .ag headers).
# A conformant implementation must satisfy every non-blocked test.
schema = 1

[[test]]
id = "lex_comment_fstring"
section = "00_lexical"
path = "tests/00_lexical/lex_comment_fstring.ag"
expect = "accept"
spec = "§2 (line comments; f-string interpolation; INT/FLOAT literals)"
note = "lexical basics that survive into v1.0 unchanged."

[[test]]
id = "lex_contextual_word_as_identifier"
section = "00_lexical"
path = "tests/00_lexical/lex_contextual_word_as_identifier.ag"
expect = "accept"
spec = "§2 (contextual words `as`/`by`/`reach`/`origin` lex as identifiers out of position)"
note = "`by` outside a gate position is an ordinary identifier."

[[test]]
id = "lex_reject_fstring_escaped_brace"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_fstring_escaped_brace.ag"
expect = "reject"
error = "LexError"
spec = "§2 (f-string escapes are \\n \\t \\\" \\\\ and \\${; plain braces are already literal, so \\{ is an invalid escape)"

[[test]]
id = "lex_reject_keyword_as_identifier"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_keyword_as_identifier.ag"
expect = "reject"
error = "ParseError"
spec = "§2 (reserved keywords may not be used as identifiers)"
note = "`agent` is reserved."

[[test]]
id = "lex_reject_leading_dot_number"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_leading_dot_number.ag"
expect = "reject"
error = "ParseError"
spec = "§2 (Float is decimal digits with a point; `.5` is not a numeric literal)"

[[test]]
id = "lex_reject_malformed_fstring"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_malformed_fstring.ag"
expect = "reject"
error = "LexError"
spec = "§2 (an f-string ${ interpolation must be closed by } before the string ends)"

[[test]]
id = "lex_reject_missing_semicolon"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_missing_semicolon.ag"
expect = "reject"
error = "ParseError"
spec = "§2 (statement terminator `;` is explicit and required)"

[[test]]
id = "lex_reject_tilde"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_tilde.ag"
expect = "reject"
error = "ParseError"
spec = "§2 (there is no similarity operator; `~` lexes to an Op but the parser rejects it — a ParseError, not a LexError)"
note = "similarity is reached through memory recall or a graded judgment (§10), never a `~` operator"

[[test]]
id = "lex_reject_trailing_dot_number"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_trailing_dot_number.ag"
expect = "reject"
error = "ParseError"
spec = "§2 (Float is decimal digits with a point and digits; `1.` is not a numeric literal)"

[[test]]
id = "lex_reject_unknown_operator"
section = "00_lexical"
path = "tests/00_lexical/lex_reject_unknown_operator.ag"
expect = "reject"
error = "LexError"
spec = "§2 (operators outside the specified surface are rejected)"

[[test]]
id = "lex_string_escapes"
section = "00_lexical"
path = "tests/00_lexical/lex_string_escapes.ag"
expect = "accept"
spec = "§2 (string escapes \\n \\t \\\" \\\\ are recognized inside a string literal)"

[[test]]
id = "axes_collapse_is_decision"
section = "01_axes"
path = "tests/01_axes/axes_collapse_is_decision.ag"
expect = "accept"
spec = "§13, §15.2 (`decide c by R` collapses a Credence to a settled, ledgered Decision)"

[[test]]
id = "axes_credence_is_graded_judgment"
section = "01_axes"
path = "tests/01_axes/axes_credence_is_graded_judgment.ag"
expect = "accept"
spec = "§1, §8 (a semantic judgment is a provider send bound to Credence<E> → graded)"
note = "the v1.0 replacement for the retired `~`: a graded judgment."

[[test]]
id = "axes_pure_call_settled"
section = "01_axes"
path = "tests/01_axes/axes_pure_call_settled.ag"
expect = "accept"
spec = "§1 Axis A/B (a pure `pure` fn reaches no dependency and stays `settled`)"
note = "arithmetic only; provably dependency-free."

[[test]]
id = "axes_send_reply_is_raw"
section = "01_axes"
path = "tests/01_axes/axes_send_reply_is_raw.ag"
expect = "accept"
spec = "§1 Axis B/C, §15.3.2 T-Send (`d <- p` is on the ledger; its reply is `raw`)"
note = "a send is a ledger op yielding a raw (unstructured) reply."

[[test]]
id = "type_action_multifield_arity_reject"
section = "03_types"
path = "tests/03_types/type_action_multifield_arity_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §13 (perform arity must match the declared action signature)"

[[test]]
id = "type_action_multifield_payload_ok"
section = "03_types"
path = "tests/03_types/type_action_multifield_payload_ok.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)", "Charge(a)"]
spec = "§3, §13 (perform invocation uses positional fields in declaration order)"

[[test]]
id = "type_action_multifield_type_reject"
section = "03_types"
path = "tests/03_types/type_action_multifield_type_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §13 (perform arguments are type-checked positionally)"

[[test]]
id = "type_action_payload_type_mismatch_reject"
section = "03_types"
path = "tests/03_types/type_action_payload_type_mismatch_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (perform payload must match the declared action signature)"

[[test]]
id = "type_array_literal_and_type"
section = "03_types"
path = "tests/03_types/type_array_literal_and_type.ag"
expect = "accept"
spec = "§3, §15.2 (array<T> is the collection type; an array literal binds to an array<T> slot)"

[[test]]
id = "type_collapse_requires_rule_reject"
section = "03_types"
path = "tests/03_types/type_collapse_requires_rule_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§3, §15.2 (a gate requires its rule; `c by` with no rule is a ParseError)"

[[test]]
id = "type_credence_only_from_seam_reject"
section = "03_types"
path = "tests/03_types/type_credence_only_from_seam_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §8 (Credence<E> is produced ONLY by a provider judgment, never constructed literally)"

[[test]]
id = "type_enum_decl_and_gate"
section = "03_types"
path = "tests/03_types/type_enum_decl_and_gate.ag"
expect = "accept"
spec = "§3, §11, §13 (enum declaration; a gate decision branched with an if-chain over its variants)"
note = "a user enum, judged, gated, and branched over its variants."

[[test]]
id = "type_event_decl"
section = "03_types"
path = "tests/03_types/type_event_decl.ag"
expect = "accept"
spec = "§3 (custom ledger-event declaration with a typed payload)"

[[test]]
id = "type_event_multifield_arity_reject"
section = "03_types"
path = "tests/03_types/type_event_multifield_arity_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (emit arity must match the declared event signature)"

[[test]]
id = "type_event_multifield_payload_ok"
section = "03_types"
path = "tests/03_types/type_event_multifield_payload_ok.ag"
expect = "accept"
ledger = ["Pair"]
spec = "§3 (event invocation uses positional fields in declaration order)"

[[test]]
id = "type_event_multifield_type_reject"
section = "03_types"
path = "tests/03_types/type_event_multifield_type_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (emit arguments are type-checked positionally)"

[[test]]
id = "type_event_null_no_reply"
section = "03_types"
path = "tests/03_types/type_event_null_no_reply.ag"
expect = "accept"
spec = "§3 (null = sent, no typed reply bound)"

[[test]]
id = "type_no_text_to_principal_reject"
section = "03_types"
path = "tests/03_types/type_no_text_to_principal_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §13 (a principal basis must be a declared `principal`, not a string; `decide c by \"alice\"` has no text -> Principal coercion)"

[[test]]
id = "type_rule_not_first_class_reject"
section = "03_types"
path = "tests/03_types/type_rule_not_first_class_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (Rule is the gate parameter, not a first-class user-declared storage type)"

[[test]]
id = "type_scalars"
section = "03_types"
path = "tests/03_types/type_scalars.ag"
expect = "accept"
spec = "§3 (scalars int/float/bool/text/null)"

[[test]]
id = "type_struct_decl_and_literal"
section = "03_types"
path = "tests/03_types/type_struct_decl_and_literal.ag"
expect = "accept"
spec = "§3 (struct declaration + struct literal supplying all fields)"
note = "a declared struct constructed with every field."

[[test]]
id = "type_struct_extra_field_reject"
section = "03_types"
path = "tests/03_types/type_struct_extra_field_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §8 (struct literals/schema objects are exact; extra fields are rejected)"

[[test]]
id = "type_struct_missing_field_reject"
section = "03_types"
path = "tests/03_types/type_struct_missing_field_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (all struct fields required; no optional-by-omission)"

[[test]]
id = "type_undeclared_emit_reject"
section = "03_types"
path = "tests/03_types/type_undeclared_emit_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (events are not self-declaring; emit of an undeclared type is a TypeError)"
note = "emit of a plain event needs no power, but the type must be declared."

[[test]]
id = "type_undeclared_function_call_reject"
section = "03_types"
path = "tests/03_types/type_undeclared_function_call_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§4, §8 (a bare call to an undeclared name is a TypeError — functions, like events, are not self-declaring)"

[[test]]
id = "type_undeclared_perform_reject"
section = "03_types"
path = "tests/03_types/type_undeclared_perform_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3 (actions are not self-declaring; perform of an undeclared type is a TypeError)"

[[test]]
id = "fn_local_memory_descriptor_reject"
section = "04_functions"
path = "tests/04_functions/fn_local_memory_descriptor_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§9, §10, §15.2 (a qualified memory descriptor is structural agent state and cannot be declared inside a function)"
note = "pure code cannot acquire a handler-local memory capability; move the descriptor to the direct agent body"

[[test]]
id = "fn_pure_calls_async_reject"
section = "04_functions"
path = "tests/04_functions/fn_pure_calls_async_reject.ag"
expect = "reject"
error = "ColorViolation"
spec = "§4 (a pure fn may only call other pure fns)"

[[test]]
id = "fn_pure_decide_by_principal_reject"
section = "04_functions"
path = "tests/04_functions/fn_pure_decide_by_principal_reject.ag"
expect = "reject"
error = "ColorViolation"
spec = "§4, §13 (`decide c by p` for a principal reaches the identity dependency → async; a pure fn may not)"

[[test]]
id = "fn_pure_emit_ok"
section = "04_functions"
path = "tests/04_functions/fn_pure_emit_ok.ag"
expect = "accept"
spec = "§4 (emit is a ledger append, permitted in pure; a plain event needs no power)"

[[test]]
id = "fn_pure_inhand_decide_ok"
section = "04_functions"
path = "tests/04_functions/fn_pure_inhand_decide_ok.ag"
expect = "accept"
spec = "§4, §13 (a rule-driven `decide` over an in-hand Credence is a pure collapse, no dependency reach → pure-permitted)"
note = "a pure fn may rule-decide a Credence value already in hand."

[[test]]
id = "fn_pure_local_ok"
section = "04_functions"
path = "tests/04_functions/fn_pure_local_ok.ag"
expect = "accept"
spec = "§4 (a pure fn that reaches no declared dependency is well-formed)"

[[test]]
id = "fn_pure_reaches_seam_reject"
section = "04_functions"
path = "tests/04_functions/fn_pure_reaches_seam_reject.ag"
expect = "reject"
error = "ColorViolation"
spec = "§1 Axis A, §4 (a pure fn may not reach the provider via `<-`)"

[[test]]
id = "fn_reject_nonterminal_return"
section = "04_functions"
path = "tests/04_functions/fn_reject_nonterminal_return.ag"
expect = "reject"
error = "TypeError"
spec = "§4 (`return` is honored in tail position only — the final statement of a function body; a `return` nested in an `if` is never evaluated and would be silently ignored, so it is a static error, not a runtime no-op)"

[[test]]
id = "fn_taint_flows_through_call_reject"
section = "04_functions"
path = "tests/04_functions/fn_taint_flows_through_call_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§15.3.3 (function calls are trust-transparent; taint flowing through a helper still cannot reach a consequential sink)"

[[test]]
id = "agent_crash_contained"
section = "05_agents"
path = "tests/05_agents/agent_crash_contained.ag"
expect = "accept"
contains = ["AgentCrashed(a)"]
provider = "empty"
spec = "§5 (an unrecoverable seam failure — the provider returns nothing — crashes the agent: AgentCrashed is recorded, the on-crash hook runs, and state survives; a crash is contained, not a death)"

[[test]]
id = "agent_extend_inherits_when"
section = "05_agents"
path = "tests/05_agents/agent_extend_inherits_when.ag"
expect = "accept"
spec = "§5, §7 (extend inherits fields + constructor + when blocks + hooks)"
note = "the child inherits Base's typed when subscription."

[[test]]
id = "agent_first_awake_runs_constructor"
section = "05_agents"
path = "tests/05_agents/agent_first_awake_runs_constructor.ag"
expect = "accept"
contains = ["Spawned(a)", "AgentAwake(a)"]
spec = "§5 (first awake opens the mailbox and runs the on-awake hook)"

[[test]]
id = "agent_instruction_extend_append_accept"
section = "05_agents"
path = "tests/05_agents/agent_instruction_extend_append_accept.ag"
expect = "accept"
spec = "§5 (global, parent, and child instructions compose in order; extend appends behavior instead of weakening the parent)"

[[test]]
id = "agent_instruction_global_accept"
section = "05_agents"
path = "tests/05_agents/agent_instruction_global_accept.ag"
expect = "accept"
spec = "§5 (a top-level `instruction` is the global compile-time system prompt)"

[[test]]
id = "agent_instruction_requires_string_reject"
section = "05_agents"
path = "tests/05_agents/agent_instruction_requires_string_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§5 (`instruction` takes a string literal — the system prompt)"

[[test]]
id = "agent_instruction_scoped_accept"
section = "05_agents"
path = "tests/05_agents/agent_instruction_scoped_accept.ag"
expect = "accept"
spec = "§5 (an agent-scoped `instruction` composes after the global block)"

[[test]]
id = "agent_prompt_ingress_to_provider_deny"
section = "05_agents"
path = "tests/05_agents/agent_prompt_ingress_to_provider_deny.ag"
expect = "reject"
error = "TaintViolation"
manifest = ["security.tainted_ingress_to_provider=deny", "prompts.question.driver=stdin"]
spec = "§5b, §17 (deny-mode provider-prompt ingress policy rejects unscreened prompt ingress before it reaches cognition)"

[[test]]
id = "agent_prompt_ingress_to_provider_warn"
section = "05_agents"
path = "tests/05_agents/agent_prompt_ingress_to_provider_warn.ag"
expect = "accept"
warning = ["tainted_ingress_to_provider"]
manifest = ["prompts.question.driver=stdin"]
spec = "§5b, §17 (default provider-prompt ingress policy warns when judgment-settled but unscreened prompt ingress is interpolated into cognition)"

[[test]]
id = "agent_prompt_opens_sensor"
section = "05_agents"
path = "tests/05_agents/agent_prompt_opens_sensor.ag"
expect = "accept"
contains = ["PromptOpened(question)"]
spec = "§5b (a `prompt` declaration opens a standing external input sensor)"

[[test]]
id = "agent_prompt_screened_ingress_to_provider_ok"
section = "05_agents"
path = "tests/05_agents/agent_prompt_screened_ingress_to_provider_ok.ag"
expect = "accept"
absent_warning = ["tainted_ingress_to_provider"]
manifest = ["security.tainted_ingress_to_provider=deny", "prompts.question.driver=stdin", "security.ingress.prompts.question.driver=mock-screen"]
spec = "§5b, §17 (a manifest-screened prompt ingress value is external_screened, so it may feed cognition without warning even under deny mode)"

[[test]]
id = "agent_prompt_value_drives_perform_ok"
section = "05_agents"
path = "tests/05_agents/agent_prompt_value_drives_perform_ok.ag"
expect = "accept"
spec = "§5b, §13 (a prompt value is judgment-settled external ingress and may drive a perform)"

[[test]]
id = "agent_reawake_no_reconstruct"
section = "05_agents"
path = "tests/05_agents/agent_reawake_no_reconstruct.ag"
expect = "accept"
spec = "§5 (re-awake resumes the agent: no re-bind, no re-construct)"

[[test]]
id = "agent_sleep_runs_hook"
section = "05_agents"
path = "tests/05_agents/agent_sleep_runs_hook.ag"
expect = "accept"
contains = ["SleepEvent(a)"]
spec = "§5 (sleep closes the mailbox and runs the on-sleep hook)"

[[test]]
id = "agent_spawn_instantiate_only"
section = "05_agents"
path = "tests/05_agents/agent_spawn_instantiate_only.ag"
expect = "accept"
contains = ["Spawned(a)"]
absent = ["AgentAwake(a)"]
spec = "§5 (spawn instantiates + constructs only: no mailbox, no awake hook)"

[[test]]
id = "when_reaction_crash_contained"
section = "05_agents"
path = "tests/05_agents/when_reaction_crash_contained.ag"
expect = "accept"
absent = ["Handled"]
order = ["AgentCrashed(a)", "Recovered(a)"]
provider = "empty"
spec = "§16.6 (the reaction boundary: a `when`-body firing is a handler invocation just like `on awake`; an uncaught fault is CONTAINED — AgentCrashed is recorded and the on-crash hook runs with state intact — never propagated out of the run)"

[[test]]
id = "comm_expiry_tombstone"
section = "06_communication"
path = "tests/06_communication/comm_expiry_tombstone.ag"
expect = "accept"
absent = ["Delivered(s)"]
order = ["Sent(s)", "Expired(s)"]
spec = "§6 (a send whose `expires` lifetime elapses before Delivered appends an Expired tombstone; no Delivered follows)"
note = "Worker is never awakened, so the 1-tick lifetime elapses undelivered"

[[test]]
id = "comm_late_delivery_refused"
section = "06_communication"
path = "tests/06_communication/comm_late_delivery_refused.ag"
expect = "accept"
contains = ["Expired(s)", "DeliveryRefused(s)"]
absent = ["Delivered(s)"]
spec = "§6, §9 (a delivery attempt after expiry records DeliveryRefused, not Delivered, and is not an Error)"

[[test]]
id = "comm_lifecycle_order"
section = "06_communication"
path = "tests/06_communication/comm_lifecycle_order.ag"
expect = "accept"
order = ["Sent(s)", "Delivered(s)", "Resolved(s)"]
spec = "§6 (a delivered send moves through Sent → Delivered → Resolved, in that order, each a ledger event correlated by corr)"

[[test]]
id = "comm_self_send_thinks"
section = "06_communication"
path = "tests/06_communication/comm_self_send_thinks.ag"
expect = "accept"
spec = "§6 (sending to self is the agent's own cognition; needs no reach grant)"

[[test]]
id = "comm_send_expires_ok"
section = "06_communication"
path = "tests/06_communication/comm_send_expires_ok.ag"
expect = "accept"
spec = "§6 (a send may carry a lifetime: `dest <- msg expires N`; reach into Worker is granted)"

[[test]]
id = "comm_send_lost_no_delivery"
section = "06_communication"
path = "tests/06_communication/comm_send_lost_no_delivery.ag"
expect = "accept"
contains = ["Sent(s)"]
absent = ["Delivered"]
spec = "§6 (a send to a non-awake agent is lost — the chain stalls at Sent, never Delivered; loss is the absence of Delivered, not an event)"
note = "Worker is spawned but never awakened, so it has no mailbox; the send is a queryable orphan"

[[test]]
id = "comm_typed_reply"
section = "06_communication"
path = "tests/06_communication/comm_typed_reply.ag"
expect = "accept"
spec = "§6 (a typed reply binds the provider answer into a typed value; the send lifecycle is ledgered)"

[[test]]
id = "world_emit_wired_result_taint_join_reject"
section = "06b_world"
path = "tests/06b_world/world_emit_wired_result_taint_join_reject.ag"
expect = "reject"
error = "TaintViolation"
manifest = ["tools.t1.driver=mock", "events.Q.tool=t1", "events.Q.result_event=Answer"]
spec = "§6b, §13 (no laundering: a result_event payload carries the JOIN of the triggering emit's payload trust — a raw query taints its own results, which then cannot drive a consequential perform un-gated)"

[[test]]
id = "world_emit_wired_tainted_ok"
section = "06b_world"
path = "tests/06b_world/world_emit_wired_tainted_ok.ag"
expect = "accept"
order = ["Q", "ToolStarted(t1)", "ToolResolved(t1)", "Answer"]
manifest = ["tools.t1.driver=mock", "events.Q.tool=t1", "events.Q.result_event=Answer"]
spec = "§6b (emit is not a consequential sink, so an emit-trigger wiring is the loose observation channel: a RAW payload may flow out — an explicit, manifest-visible opt-out of the perform path's guarantee)"

[[test]]
id = "world_foreground_binding_no_result_event_reject"
section = "06b_world"
path = "tests/06b_world/world_foreground_binding_no_result_event_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["tools.t1.driver=mock", "actions.Ask.tool=t1"]
spec = "§6b, §17.1 (a foreground binding on an action wired with no result_event is a ConfigError — there is nothing to bind the reply to)"

[[test]]
id = "world_foreground_binding_requires_expires_reject"
section = "06b_world"
path = "tests/06b_world/world_foreground_binding_requires_expires_reject.ag"
expect = "reject"
error = "TypeError"
manifest = ["tools.t1.driver=mock", "actions.Ask.tool=t1", "actions.Ask.result_event=Answer"]
spec = "§6b, §6c (`expires` is mandatory on the result-binding form of perform — terminal by construction, like every delegation)"

[[test]]
id = "world_foreground_perform_binding_ok"
section = "06b_world"
path = "tests/06b_world/world_foreground_perform_binding_ok.ag"
expect = "accept"
order = ["Ask", "ToolStarted(t1)", "ToolResolved(t1)", "Answer", "Notify"]
manifest = ["tools.t1.driver=mock", "actions.Ask.tool=t1", "actions.Ask.result_event=Answer"]
spec = "§6b, §13 (foreground perform binding: a wired action with a result_event binds its reply like a §6c delegation; a judgment-settled request yields judgment-settled external ingress, which may drive a further perform)"

[[test]]
id = "world_perform_unsettled_reject"
section = "06b_world"
path = "tests/06b_world/world_perform_unsettled_reject.ag"
expect = "reject"
error = "TaintViolation"
manifest = ["tools.t1.driver=mock", "actions.Ask.tool=t1"]
spec = "§6b, §13 (every perform is a consequential sink taking settled args only — anti-exfiltration: un-endorsed cognition never leaves the process on the perform path)"

[[test]]
id = "world_perform_unsettled_unwired_reject"
section = "06b_world"
path = "tests/06b_world/world_perform_unsettled_unwired_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§6b, §13 (the settled-only rule for perform args is UNIFORM: it does not depend on whether the deployment wires the action — checker semantics never depend on the manifest)"

[[test]]
id = "world_pure_perform_reject"
section = "06b_world"
path = "tests/06b_world/world_pure_perform_reject.ag"
expect = "reject"
error = "ColorViolation"
spec = "§4, §6b (every perform is async — whether an act reaches the world is a deployment fact the checker must not depend on; a `pure` function may not perform)"

[[test]]
id = "world_replay_chain_head"
section = "06b_world"
path = "tests/06b_world/world_replay_chain_head.ag"
expect = "accept"
contains = ["pair(Tool@t1)"]
manifest = ["tools.t1.driver=mock", "actions.Pay.tool=t1"]
replay = "chain_head_equal"
spec = "§6b, §16.5 (recorded replay of a wired perform regenerates the same chain-head: the seam's journal pair re-serves the endpoint result and nothing is re-invoked)"

[[test]]
id = "world_result_event_ingress_to_provider_warn"
section = "06b_world"
path = "tests/06b_world/world_result_event_ingress_to_provider_warn.ag"
expect = "accept"
warning = ["tainted_ingress_to_provider"]
manifest = ["tools.t1.driver=mock", "actions.Search.tool=t1", "actions.Search.result_event=SearchResult"]
spec = "§6b, §17 (default provider-prompt ingress policy warns when unscreened result-event ingress is interpolated into cognition)"

[[test]]
id = "world_result_event_screened_ingress_to_provider_ok"
section = "06b_world"
path = "tests/06b_world/world_result_event_screened_ingress_to_provider_ok.ag"
expect = "accept"
absent_warning = ["tainted_ingress_to_provider"]
manifest = ["security.tainted_ingress_to_provider=deny", "tools.t1.driver=mock", "actions.Search.tool=t1", "actions.Search.result_event=SearchResult", "security.ingress.events.SearchResult.accepted=true"]
spec = "§6b, §17 (a manifest-screened result-event ingress value is external_screened, so it may feed cognition without warning even under deny mode)"

[[test]]
id = "world_unwired_action_pure_ok"
section = "06b_world"
path = "tests/06b_world/world_unwired_action_pure_ok.ag"
expect = "accept"
contains = ["Announce"]
absent = ["ToolStarted", "ToolResolved"]
spec = "§6b (unwired = pure: an unwired action is a ledgered performative — the act is the record; no seam journal pair appears)"

[[test]]
id = "world_wired_perform_invokes_ok"
section = "06b_world"
path = "tests/06b_world/world_wired_perform_invokes_ok.ag"
expect = "accept"
contains = ["pair(Tool@t1)"]
order = ["Ping", "ToolStarted(t1)", "ToolResolved(t1)"]
manifest = ["tools.t1.driver=mock", "actions.Ping.tool=t1"]
spec = "§6b (an [actions.NAME] wiring makes perform invoke the catalog endpoint: the action's own domain row, then the seam's ToolStarted/ToolResolved journal pair correlated by catalog name)"

[[test]]
id = "world_wired_perform_result_event_ok"
section = "06b_world"
path = "tests/06b_world/world_wired_perform_result_event_ok.ag"
expect = "accept"
order = ["Ask", "ToolStarted(t1)", "ToolResolved(t1)", "Answer", "Got"]
manifest = ["tools.t1.driver=mock", "actions.Ask.tool=t1", "actions.Ask.result_event=Answer"]
spec = "§6b (a wiring's result_event lands the endpoint's reply as the named event row after the journal pair; a statement-form perform consumes it reactively via `when`)"

[[test]]
id = "del_background_expiry_alias"
section = "06c_delegation"
path = "tests/06c_delegation/del_background_expiry_alias.ag"
expect = "accept"
contains = ["Expired(h)", "ExpirySeen"]
absent = ["TaskExpired", "AgentCrashed(lead)"]
spec = "§6c (TaskExpired is a subscription ALIAS for Expired filtered to task-sends — no row of its own; a background expiry never faults the delegator)"

[[test]]
id = "del_background_when_completed"
section = "06c_delegation"
path = "tests/06c_delegation/del_background_when_completed.ag"
expect = "accept"
contains = ["TaskCompleted(h)", "Landed"]
spec = "§6c (a background task-send binds a settled Task<T> handle; the outcome is observed with `when (TaskCompleted … about h)`; delivery may span ticks — §6)"

[[test]]
id = "del_cancel_after_terminal_noop"
section = "06c_delegation"
path = "tests/06c_delegation/del_cancel_after_terminal_noop.ag"
expect = "accept"
contains = ["TaskCompleted(h)"]
absent = ["TaskCancelled(h)"]
spec = "§6c, §16.3a (the first terminal wins: cancel of an already-terminal task appends nothing)"

[[test]]
id = "del_cancel_non_handle_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_cancel_non_handle_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c, §15.3.3 (`cancel` takes a Task<T> handle)"

[[test]]
id = "del_cancel_tombstone"
section = "06c_delegation"
path = "tests/06c_delegation/del_cancel_tombstone.ag"
expect = "accept"
contains = ["TaskCancelled(h)"]
absent = ["Delivered(h)", "TaskCompleted(h)", "Expired(h)"]
spec = "§6c (cancel appends the authoritative TaskCancelled tombstone; the first terminal wins — a cancelled task neither delivers nor expires)"
note = "Worker is never awakened; cancel lands before any delivery could occur"

[[test]]
id = "del_complete_outside_task_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_complete_outside_task_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c, §15.3.3 (`complete`/`fail` are legal only inside a task handler)"

[[test]]
id = "del_empty_task_block_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_empty_task_block_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (an empty task block is a compile error — objective and acceptance are required)"

[[test]]
id = "del_endorsed_completion_settled_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_endorsed_completion_settled_ok.ag"
expect = "accept"
order = ["Endorsed", "TaskCompleted(body)", "Publish"]
spec = "§6c (a worker that completes with an Endorsement<T> hands over a settled, ledger-backed subject — the delegator may sink it directly)"

[[test]]
id = "del_endorsed_scoped_perform_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_endorsed_scoped_perform_ok.ag"
expect = "accept"
order = ["Endorsed", "Delivered(r)", "Deploy", "TaskCompleted(r)"]
spec = "§6c, §13 (the canonical scoped flow: draft task → decide → endorse in the committed branch → send Endorsement<TaskSpec> → the worker's statically-granted perform is task-enabled)"

[[test]]
id = "del_expires_settled_expr_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_expires_settled_expr_ok.ag"
expect = "accept"
contains = ["TaskCompleted(r)"]
spec = "§6, §6c (`expires` accepts any settled numeric expression, not only a literal)"

[[test]]
id = "del_expires_unsettled_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_expires_unsettled_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§6, §6c (`expires` requires a SETTLED numeric expression; a cognition-derived lifetime is rejected)"

[[test]]
id = "del_expiry_faults_foreground"
section = "06c_delegation"
path = "tests/06c_delegation/del_expiry_faults_foreground.ag"
expect = "accept"
absent = ["Delivered(r)", "Done"]
order = ["Sent(r)", "Expired(r)", "AgentCrashed(lead)", "Recovered"]
spec = "§6c, §16.6 (mandatory expiry converts a lost task-send into a signal: the Expired tombstone faults the awaiting foreground invocation)"
note = "Worker is never awakened, so the 1-tick lifetime elapses undelivered"

[[test]]
id = "del_fanout_shared_worker_concurrent_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_fanout_shared_worker_concurrent_ok.ag"
expect = "accept"
order = ["Spawned(worker)", "TaskCompleted(r)", "TaskCompleted(r)", "TaskCompleted(r)", "Done"]
replay = "chain_head_equal"
spec = "§6c + §12 — fan-out delegation composes with `|>`: a delegating function mapped over a finite collection to a SHARED worker runs its foreground tasks concurrently (paths overlap, §12), all complete, and the ledger replays identically (§0.2 determinism is serialized EFFECTS, not serialized execution — no shared mutable state to race over)"

[[test]]
id = "del_fanout_spawn_expr_distinct_workers_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_fanout_spawn_expr_distinct_workers_ok.ag"
expect = "accept"
order = ["TaskCompleted(r)", "TaskCompleted(r)", "TaskCompleted(r)", "Done"]
replay = "chain_head_equal"
spec = "§6c + §12 + §15.4 — the `spawn` EXPRESSION (`Worker w = spawn Worker;`) mints a FRESH distinct worker per fan-out element, so `xs |> f` builds a dynamic collection of workers; each verifies one delegated task, and instance names are derived from (call-site, element index), not execution order, so the ledger replays byte-identically"

[[test]]
id = "del_foreground_complete_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_foreground_complete_ok.ag"
expect = "accept"
order = ["Sent(r)", "Delivered(r)", "Resolved(r)", "TaskCompleted(r)", "Done"]
spec = "§6c (a foreground task-send: the worker's `complete` produces the transport Resolved plus TaskCompleted, and the delegator's continuation resumes with the result)"

[[test]]
id = "del_foreground_failure_faults"
section = "06c_delegation"
path = "tests/06c_delegation/del_foreground_failure_faults.ag"
expect = "accept"
absent = ["Done"]
order = ["TaskFailed(r)", "AgentCrashed(lead)", "Recovered"]
spec = "§6c, §16.6 (a foreground task terminal other than TaskCompleted faults the delegator's awaiting invocation via the contained-crash path; `on crash` recovers with state intact)"

[[test]]
id = "del_missing_expires_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_missing_expires_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (`expires` is mandatory on every delegation — every task is terminal by construction)"

[[test]]
id = "del_no_assigned_handler_expires"
section = "06c_delegation"
path = "tests/06c_delegation/del_no_assigned_handler_expires.ag"
expect = "accept"
absent = ["TaskCompleted(h)", "AgentCrashed(lead)"]
order = ["Sent(h)", "Delivered(h)", "Expired(h)"]
spec = "§6c (an assigned task with no completing handler is not an error; the mandatory expiry backstops it with a tombstone)"

[[test]]
id = "del_objective_missing_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_objective_missing_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (a task literal requires BOTH `objective` and `acceptance`)"

[[test]]
id = "del_objective_not_text_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_objective_not_text_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (`objective` and `acceptance` must be `text`)"

[[test]]
id = "del_perform_unscoped_task_faults"
section = "06c_delegation"
path = "tests/06c_delegation/del_perform_unscoped_task_faults.ag"
expect = "accept"
contains = ["TaskScopeViolation(w)", "AgentCrashed(w)"]
absent = ["Deploy", "TaskCompleted(h)"]
spec = "§6c, §13, §16.6 (a perform inside an assigned task requires the active task to be endorsed AND to name the action in scope; a plain task enables nothing — the action faults with TaskScopeViolation and does not run)"

[[test]]
id = "del_progress_outside_task_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_progress_outside_task_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (TaskProgress is emittable only inside a task handler — it correlates to the active task)"

[[test]]
id = "del_progress_then_cancelled_hook"
section = "06c_delegation"
path = "tests/06c_delegation/del_progress_then_cancelled_hook.ag"
expect = "accept"
absent = ["TaskCompleted(h)"]
order = ["Sent(h)", "Delivered(h)", "TaskProgress(h)", "TaskCancelled(h)", "CancelAck"]
spec = "§6c (TaskProgress is the repeatable worker event; cancel mid-task is cooperative — the worker's `on cancelled` hook fires, nothing is preempted)"

[[test]]
id = "del_reach_required_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_reach_required_reject.ag"
expect = "reject"
error = "AuthorityViolation"
spec = "§6c, §13 (delegation is a send: reaching a worker requires the `reach` power, default-deny)"

[[test]]
id = "del_result_raw_to_sink_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_result_raw_to_sink_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§6c, §13 (a delegated result is RAW by default — delegation never launders trust; it cannot drive a consequential sink un-gated)"

[[test]]
id = "del_scope_not_held_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_scope_not_held_reject.ag"
expect = "reject"
error = "AuthorityViolation"
spec = "§6c, §15.3.3 W-Scope-Attenuate (a task scope can only ATTENUATE the delegator's authority — each scoped action must be held by the delegator itself)"

[[test]]
id = "del_scope_unendorsed_send_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_scope_unendorsed_send_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§6c, §15.3.3 W-Scope-Attenuate (a task carrying a `scope` clause is enabling only when endorsed; sending an unendorsed scoped task is rejected)"

[[test]]
id = "del_tainted_objective_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_tainted_objective_ok.ag"
expect = "accept"
contains = ["TaskCompleted(r)"]
spec = "§6c (a generated objective stays tainted but an UNSCOPED task may still be sent — taint matters at sinks, and a plain task grants nothing)"

[[test]]
id = "del_taskspec_draft_binding_ok"
section = "06c_delegation"
path = "tests/06c_delegation/del_taskspec_draft_binding_ok.ag"
expect = "accept"
contains = ["TaskCompleted(r)"]
spec = "§6c (a task literal is an expression: a TaskSpec draft may be bound first and sent later; expires stays mandatory at the send)"

[[test]]
id = "del_unbound_statement_reject"
section = "06c_delegation"
path = "tests/06c_delegation/del_unbound_statement_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§6c (bare statement-form delegation is a compile error — hold the result or the Task<T> handle; every task is addressable)"

[[test]]
id = "del_worker_fail_records"
section = "06c_delegation"
path = "tests/06c_delegation/del_worker_fail_records.ag"
expect = "accept"
contains = ["TaskFailed(h)", "FailSeen"]
absent = ["Resolved(h)", "TaskCompleted(h)"]
spec = "§6c (`fail reason` appends TaskFailed; the transport chain rests at its Delivered prefix — a stalled prefix is not a violation; a background delegator observes it with `when`)"

[[test]]
id = "ledger_exact_spine"
section = "07_ledger"
path = "tests/07_ledger/ledger_exact_spine.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)"]
spec = "§7, §16.2 (ledger assertions can pin the exact ordered event spine, with no extra events)"

[[test]]
id = "ledger_multi_handler_order"
section = "07_ledger"
path = "tests/07_ledger/ledger_multi_handler_order.ag"
expect = "accept"
order = ["First(a)", "Second(a)"]
spec = "§7 (when several subscriptions match one appended event in one tick, they fire in registration/hoist order — within a scope, lexical order)"

[[test]]
id = "ledger_prospective_only"
section = "07_ledger"
path = "tests/07_ledger/ledger_prospective_only.ag"
expect = "accept"
absent = ["Event"]
spec = "§7 (subscriptions are prospective; never fire for prior events)"
note = "a when registered after an emit does NOT fire for that earlier event."

[[test]]
id = "ledger_query_result_event"
section = "07_ledger"
path = "tests/07_ledger/ledger_query_result_event.ag"
expect = "accept"
contains = ["QueryResult(ledger)"]
spec = "§10 (a query STATEMENT lands a QueryResult event on the ledger)"

[[test]]
id = "ledger_tool_pair"
section = "07_ledger"
path = "tests/07_ledger/ledger_tool_pair.ag"
expect = "accept"
contains = ["pair(Tool@search)"]
manifest = ["tools.search.driver=mock", "actions.Search.tool=search"]
spec = "§6b, §7 (a wired perform appends the seam's ToolStarted/ToolResolved journal pair beneath the action's domain row, correlated by catalog name)"

[[test]]
id = "ledger_when_about_filters"
section = "07_ledger"
path = "tests/07_ledger/ledger_when_about_filters.ag"
expect = "accept"
contains = ["Got(a)"]
spec = "§7 (a `when (Type b about subj)` fires only for events about the held subject; the bound event evaluates to its payload)"

[[test]]
id = "ledger_when_guard_ok"
section = "07_ledger"
path = "tests/07_ledger/ledger_when_guard_ok.ag"
expect = "accept"
spec = "§7 (a `when … if (guard)` filters by an ordinary predicate over the bound event's fields)"

[[test]]
id = "sem_contradiction_emits_event"
section = "08_semantic"
path = "tests/08_semantic/sem_contradiction_emits_event.ag"
expect = "accept"
contains = ["Contradiction(j)"]
provider = "credence(Contradicts=0.9, Entails=0.05, Neutral=0.05)"
spec = "§8, §11 (committing a Credence<Entailment> to Contradicts also emits a first-class Contradiction)"

[[test]]
id = "sem_credence_over_user_enum"
section = "08_semantic"
path = "tests/08_semantic/sem_credence_over_user_enum.ag"
expect = "accept"
spec = "§8 (a provider send bound to Credence<E> is a constrained classifier over E)"

[[test]]
id = "sem_entailment_three_valued"
section = "08_semantic"
path = "tests/08_semantic/sem_entailment_three_valued.ag"
expect = "accept"
spec = "§8, §9 (Credence<Entailment> over {Entails, Contradicts, Neutral})"

[[test]]
id = "sem_sample_unknown_reject"
section = "08_semantic"
path = "tests/08_semantic/sem_sample_unknown_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§8 (there is no sampling combinator in the surface language; a use of `sample` is an unknown-identifier error)"

[[test]]
id = "sem_schema_array_nested_exact"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_array_nested_exact.ag"
expect = "accept"
schema = ["array<Memo> array(object(body:text,required:body,additionalProperties:false))"]
spec = "§8 (array<T> compiles to an array schema whose item schema is the exact schema for T)"

[[test]]
id = "sem_schema_enum_exact"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_enum_exact.ag"
expect = "accept"
schema = ["Ticket closed_enum(Billing,Bug)"]
spec = "§8 (Enum compiles to a closed enum structured-output schema)"

[[test]]
id = "sem_schema_struct_exact"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_struct_exact.ag"
expect = "accept"
schema = ["Memo object(amount:int,to:text,required:amount|to,additionalProperties:false)"]
spec = "§8 (struct compiles to an exact object schema with all fields required and no extra fields)"

[[test]]
id = "sem_schema_violation_array_typemismatch"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_violation_array_typemismatch.ag"
expect = "accept"
contains = ["TypeMismatch", "AgentCrashed(a)"]
order = ["TypeMismatch", "AgentCrashed"]
provider = "schema_violation"
spec = "§8, §16.6 (array<T> compiles to schema-constrained output; a schema failure faults the send with TypeMismatch, then the reaction crashes)"

[[test]]
id = "sem_schema_violation_enum_typemismatch"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_violation_enum_typemismatch.ag"
expect = "accept"
contains = ["TypeMismatch", "AgentCrashed(a)"]
order = ["TypeMismatch", "AgentCrashed"]
provider = "schema_violation"
spec = "§8, §16.6 (Enum compiles to a closed enum schema; a schema failure faults the send with TypeMismatch, then the reaction crashes)"

[[test]]
id = "sem_schema_violation_faults_send_on_crash"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_violation_faults_send_on_crash.ag"
expect = "accept"
contains = ["TypeMismatch", "AgentCrashed(a)", "Recovered(a)"]
absent = ["Resolved"]
order = ["TypeMismatch", "AgentCrashed", "Recovered"]
provider = "schema_violation"
spec = "§8, §16.6 (a typed reply that fails its schema faults AT the send: TypeMismatch then the reaction crashes, so the downstream field access never runs and no null enters the binding; `on crash` recovers with state intact)"

[[test]]
id = "sem_schema_violation_typemismatch"
section = "08_semantic"
path = "tests/08_semantic/sem_schema_violation_typemismatch.ag"
expect = "accept"
contains = ["TypeMismatch", "AgentCrashed(a)"]
order = ["TypeMismatch", "AgentCrashed"]
provider = "schema_violation"
spec = "§8, §16.6 (structured output uses constrained decoding; on schema failure the runtime appends a TypeMismatch and faults the send — the reaction crashes, no null enters the binding)"

[[test]]
id = "prelude_action_cannot_be_error_reject"
section = "09_prelude"
path = "tests/09_prelude/prelude_action_cannot_be_error_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§19.5 (only `event` may extend Error; an action supertype is a ParseError)"

[[test]]
id = "prelude_error_supertype_must_be_error_reject"
section = "09_prelude"
path = "tests/09_prelude/prelude_error_supertype_must_be_error_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§19.5 (the only permitted user supertype is the built-in Error)"

[[test]]
id = "prelude_expired_not_error"
section = "09_prelude"
path = "tests/09_prelude/prelude_expired_not_error.ag"
expect = "accept"
contains = ["Expired(s)"]
absent = ["Caught"]
spec = "§9 (Expired and a lost send are NOT Error subtypes; a `when (Error e)` does not fire for an Expired tombstone)"

[[test]]
id = "prelude_say_not_ledger"
section = "09_prelude"
path = "tests/09_prelude/prelude_say_not_ledger.ag"
expect = "accept"
absent = ["Event"]
spec = "§9 (`say(x)` prints its argument; it is NOT a ledger operation and appends no event)"

[[test]]
id = "prelude_user_error_subtype_caught"
section = "09_prelude"
path = "tests/09_prelude/prelude_user_error_subtype_caught.ag"
expect = "accept"
contains = ["Error"]
spec = "§19.5 (event Foo : Error extends the built-in Error root; when(Error e) catches it)"

[[test]]
id = "prelude_when_error_catches_contradiction"
section = "09_prelude"
path = "tests/09_prelude/prelude_when_error_catches_contradiction.ag"
expect = "accept"
spec = "§9 (Contradiction extends Error; when (Error e) catches it by subtype)"

[[test]]
id = "mem_authority_payload_reject"
section = "10_memory"
path = "tests/10_memory/mem_authority_payload_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (an Endorsement wrapper is authority-bearing and cannot be a persistent memory payload type)"

[[test]]
id = "mem_clause_order_accept"
section = "10_memory"
path = "tests/10_memory/mem_clause_order_accept.ag"
expect = "accept"
contains = ["Internalized(notes)", "MemoryConsulted(notes)"]
order = ["Internalized(notes)", "MemoryConsulted(notes)"]
spec = "§10, §15.3 (descriptor clause order is insignificant)"

[[test]]
id = "mem_descriptor_typed_store_recall_accept"
section = "10_memory"
path = "tests/10_memory/mem_descriptor_typed_store_recall_accept.ag"
expect = "accept"
contains = ["Internalized(episodes)", "MemoryConsulted(episodes)"]
order = ["Internalized(episodes)", "MemoryConsulted(episodes)"]
spec = "§10, §15.3 (the descriptor type governs both exact stores and TYPE[] recall)"

[[test]]
id = "mem_duplicate_clause_reject"
section = "10_memory"
path = "tests/10_memory/mem_duplicate_clause_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10, §15.3 (a descriptor clause may appear only once)"

[[test]]
id = "mem_duplicate_name_reject"
section = "10_memory"
path = "tests/10_memory/mem_duplicate_name_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10, §15.3 (one structural descriptor is permitted per handle name in an agent)"

[[test]]
id = "mem_duplicate_scope_reject"
section = "10_memory"
path = "tests/10_memory/mem_duplicate_scope_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (a scope dimension may appear only once in the descriptor tuple)"

[[test]]
id = "mem_empty_typed_recall_accept"
section = "10_memory"
path = "tests/10_memory/mem_empty_typed_recall_accept.ag"
expect = "accept"
contains = ["MemoryConsulted(notes)"]
absent = ["Sent", "Delivered", "Resolved"]
spec = "§10 (recall against an empty typed region is well-typed, records consultation, and invokes no provider)"
note = "exact [] payload semantics are asserted by the runtime-contract suite"

[[test]]
id = "mem_episodic_equal_writes_receipts_accept"
section = "10_memory"
path = "tests/10_memory/mem_episodic_equal_writes_receipts_accept.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)", "Internalized(episodes)", "Internalized(episodes)", "MemoryConsulted(episodes)"]
spec = "§10, §16.7 (each explicit episodic store evaluation records its own successful Internalized receipt)"
note = "distinct canonical episodes/origins for equal values are asserted by the runtime-contract suite"

[[test]]
id = "mem_expr_query_no_ledger_event"
section = "10_memory"
path = "tests/10_memory/mem_expr_query_no_ledger_event.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)"]
spec = "§10 (query expression form yields a value and appends no QueryResult event)"

[[test]]
id = "mem_forget_accept"
section = "10_memory"
path = "tests/10_memory/mem_forget_accept.ag"
expect = "accept"
spec = "§10 (forget closes only the current scope tuple generation while the structural descriptor survives)"

[[test]]
id = "mem_forget_records_tombstone"
section = "10_memory"
path = "tests/10_memory/mem_forget_records_tombstone.ag"
expect = "accept"
contains = ["Forgotten(notes)"]
spec = "§10 (forget appends a truthful Forgotten receipt for the resolved tuple generation)"

[[test]]
id = "mem_forget_reopen_operations_accept"
section = "10_memory"
path = "tests/10_memory/mem_forget_reopen_operations_accept.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)", "Internalized(notes)", "Forgotten(notes)", "MemoryConsulted(notes)", "Internalized(notes)", "MemoryConsulted(notes)"]
spec = "§10, §16.7 (the structural descriptor remains usable for recall and a later store after forget)"
note = "exact closed-generation misses and generation+1 isolation are asserted by the runtime-contract suite"

[[test]]
id = "mem_generic_syntax_reject"
section = "10_memory"
path = "tests/10_memory/mem_generic_syntax_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§10, §15.2 (mem<T> is not Agape syntax; payload type is a named descriptor clause)"

[[test]]
id = "mem_ledger_recall_reject"
section = "10_memory"
path = "tests/10_memory/mem_ledger_recall_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (the ledger is not private memory; `->` recall requires a `mem` handle, never `ledger`)"

[[test]]
id = "mem_legacy_direct_reject"
section = "10_memory"
path = "tests/10_memory/mem_legacy_direct_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (legacy unqualified declarations receive the qualified-descriptor migration diagnostic)"

[[test]]
id = "mem_legacy_local_initialized_reject"
section = "10_memory"
path = "tests/10_memory/mem_legacy_local_initialized_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (legacy handler-local initialized declarations receive the hoist-and-qualify migration diagnostic)"

[[test]]
id = "mem_local_descriptor_reject"
section = "10_memory"
path = "tests/10_memory/mem_local_descriptor_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§10, §15.2 (a qualified descriptor is a direct agent-body declaration, never handler-local)"

[[test]]
id = "mem_missing_clause_reject"
section = "10_memory"
path = "tests/10_memory/mem_missing_clause_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10, §15.3 (type, modality, scope, and retention are each required exactly once)"

[[test]]
id = "mem_modalities_names_no_magic_accept"
section = "10_memory"
path = "tests/10_memory/mem_modalities_names_no_magic_accept.ag"
expect = "accept"
ledger = ["Spawned(a)", "AgentAwake(a)", "Internalized(profile)", "Internalized(episodes)", "Internalized(literal)", "MemoryConsulted(profile)", "MemoryConsulted(episodes)", "MemoryConsulted(literal)"]
spec = "§10 (opaque, episodic, and semantic are declared policies; handle names have no intrinsic semantics)"

[[test]]
id = "mem_recall_after_forget_empty"
section = "10_memory"
path = "tests/10_memory/mem_recall_after_forget_empty.ag"
expect = "accept"
contains = ["Forgotten(notes)", "MemoryConsulted(notes)"]
order = ["Forgotten(notes)", "MemoryConsulted(notes)"]
spec = "§10 (a descriptor survives forget and recall of its closed tuple returns an empty typed array)"

[[test]]
id = "mem_recall_requires_mem_reject"
section = "10_memory"
path = "tests/10_memory/mem_recall_requires_mem_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (`->` recall requires a `mem` handle on the left; a non-`mem` LHS is a TypeError)"

[[test]]
id = "mem_recall_taint_perform_reject"
section = "10_memory"
path = "tests/10_memory/mem_recall_taint_perform_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§10, §13, §16.7 (the exact typed recall array is deeply raw and cannot drive a consequential sink without a gate)"

[[test]]
id = "mem_recall_wrong_binding_reject"
section = "10_memory"
path = "tests/10_memory/mem_recall_wrong_binding_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10, §15.3 (typed recall returns TYPE[] and cannot bind directly to text or Credence)"

[[test]]
id = "mem_select_boolean_ops_ok"
section = "10_memory"
path = "tests/10_memory/mem_select_boolean_ops_ok.ag"
expect = "accept"
spec = "§10 (select where conditions support comparison operators combined by boolean connectives)"

[[test]]
id = "mem_store_internalizes_ok"
section = "10_memory"
path = "tests/10_memory/mem_store_internalizes_ok.ag"
expect = "accept"
spec = "§10 (an explicit write stores a value assignable to the structural descriptor type)"

[[test]]
id = "mem_store_records_internalized"
section = "10_memory"
path = "tests/10_memory/mem_store_records_internalized.ag"
expect = "accept"
contains = ["Internalized(notes)"]
spec = "§10, §15.4.2 (a successful explicit typed write records Internalized)"

[[test]]
id = "mem_store_type_mismatch_reject"
section = "10_memory"
path = "tests/10_memory/mem_store_type_mismatch_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10, §15.3 (every stored expression must be assignable to the descriptor payload type)"

[[test]]
id = "mem_top_level_descriptor_reject"
section = "10_memory"
path = "tests/10_memory/mem_top_level_descriptor_reject.ag"
expect = "reject"
error = "ParseError"
spec = "§10, §15.2 (a qualified descriptor is structural agent state and is not a top-level declaration)"

[[test]]
id = "mem_unknown_modality_reject"
section = "10_memory"
path = "tests/10_memory/mem_unknown_modality_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (the closed modality vocabulary is opaque, episodic, and semantic; working state is not memory)"

[[test]]
id = "mem_unknown_retention_reject"
section = "10_memory"
path = "tests/10_memory/mem_unknown_retention_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (the closed retention vocabulary is session and durable)"

[[test]]
id = "mem_unknown_scope_reject"
section = "10_memory"
path = "tests/10_memory/mem_unknown_scope_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§10 (the closed authenticated scope vocabulary is project and user)"

[[test]]
id = "mem_user_scope_missing_subject_crashes"
section = "10_memory"
path = "tests/10_memory/mem_user_scope_missing_subject_crashes.ag"
expect = "accept"
contains = ["AgentCrashed(a)"]
absent = ["Internalized(notes)"]
manifest = ["memory.driver=local"]
spec = "§10, §16.7 (a user-scoped operation without κ.user follows the auditable crash path without a successful memory mutation)"
note = "runtime-contract instrumentation separately proves that the driver was not accessed"

[[test]]
id = "mem_write_recall_accept"
section = "10_memory"
path = "tests/10_memory/mem_write_recall_accept.ag"
expect = "accept"
contains = ["Internalized(notes)", "MemoryConsulted(notes)"]
order = ["Internalized(notes)", "MemoryConsulted(notes)"]
spec = "§10 (a qualified typed handle stores with <- and recalls a deeply raw TYPE[] with ->)"

[[test]]
id = "ctrl_credence_in_if_reject"
section = "11_control"
path = "tests/11_control/ctrl_credence_in_if_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §11 (a Credence<bool> is not a bool; a bare Credence in an if is a TypeError — gate it first)"

[[test]]
id = "ctrl_gate_abstain_ok"
section = "11_control"
path = "tests/11_control/ctrl_gate_abstain_ok.ag"
expect = "accept"
spec = "§11, §13 (the `abstained` sentinel is the else case when the gate commits no variant)"

[[test]]
id = "ctrl_gate_all_variants_ok"
section = "11_control"
path = "tests/11_control/ctrl_gate_all_variants_ok.ag"
expect = "accept"
spec = "§11, §13 (branch on a Decision's .committed with an if-chain over the enum variants)"

[[test]]
id = "ctrl_if_else"
section = "11_control"
path = "tests/11_control/ctrl_if_else.ag"
expect = "accept"
spec = "§11 (if/else over a bool)"

[[test]]
id = "ctrl_retry_exhausts_faults"
section = "11_control"
path = "tests/11_control/ctrl_retry_exhausts_faults.ag"
expect = "accept"
contains = ["TypeMismatch", "RetryExhausted(a)", "AgentCrashed(a)", "Recovered(a)"]
absent = ["Resolved"]
order = ["TypeMismatch", "RetryExhausted", "AgentCrashed", "Recovered"]
provider = "schema_violation"
spec = "§11, §16.6 (a persistent TypeMismatch exhausts the `retry N` block: after N attempts the runtime appends RetryExhausted and faults per the send-fault rule — the reaction crashes and `on crash` recovers)"

[[test]]
id = "ctrl_retry_first_attempt_ok"
section = "11_control"
path = "tests/11_control/ctrl_retry_first_attempt_ok.ag"
expect = "accept"
contains = ["Resolved"]
absent = ["TypeMismatch", "RetryExhausted", "AgentCrashed"]
spec = "§11 (a `retry N` block parses and runs in the core kernel; when the block's first attempt does not fault, it runs exactly once and no RetryExhausted is recorded)"

[[test]]
id = "ctrl_retry_recovers_typemismatch"
section = "11_control"
path = "tests/11_control/ctrl_retry_recovers_typemismatch.ag"
expect = "accept"
contains = ["TypeMismatch", "Resolved"]
absent = ["AgentCrashed", "RetryExhausted"]
order = ["TypeMismatch", "Resolved"]
provider = "schema_violation_once"
spec = "§11, §16.6 (a bounded `retry N` block is core: on a TypeMismatch it re-asks the provider, re-running the block; a transient schema failure recovers on the next attempt and the first attempt's TypeMismatch stays on the ledger)"

[[test]]
id = "ctrl_ungated_credence_committed_reject"
section = "11_control"
path = "tests/11_control/ctrl_ungated_credence_committed_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§3, §11 (a Credence<E> is consumed only by a gate/combinator; testing `.committed` before a gate is a TypeError)"

[[test]]
id = "agg_dependent_fuse_ok"
section = "12_aggregation"
path = "tests/12_aggregation/agg_dependent_fuse_ok.ag"
expect = "accept"
spec = "§12 (a `dependent` declaration fuses conservatively; quorum(1, …) is the at-least-one reduction)"

[[test]]
id = "agg_mixed_clusters_fuse"
section = "12_aggregation"
path = "tests/12_aggregation/agg_mixed_clusters_fuse.ag"
expect = "accept"
spec = "§12 (mixed sets compose: each `dependent` cluster fuses conservatively first, then the cluster results combine by the independent rule; coverage must be total over every pair)"

[[test]]
id = "agg_partial_dep_coverage_reject"
section = "12_aggregation"
path = "tests/12_aggregation/agg_partial_dep_coverage_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§12 (dependence coverage must be TOTAL over every pair; declaring only the pair (c1,c2) leaves (c1,c3) and (c2,c3) uncovered → TypeError, even though one declaration is present)"
note = "the weak reject tests declare ZERO dependence; this pins the harder N>=3 partial-coverage case"

[[test]]
id = "agg_pipe_fanout_ok"
section = "12_aggregation"
path = "tests/12_aggregation/agg_pipe_fanout_ok.ag"
expect = "accept"
spec = "§12, §15.2 (`xs |> f` is bounded fan-out over a finite collection)"

[[test]]
id = "agg_quorum_independent_ok"
section = "12_aggregation"
path = "tests/12_aggregation/agg_quorum_independent_ok.ag"
expect = "accept"
spec = "§12 (quorum over independent Credence<bool> judges fuses to one Credence<bool>)"

[[test]]
id = "agg_quorum_no_dep_decl_reject"
section = "12_aggregation"
path = "tests/12_aggregation/agg_quorum_no_dep_decl_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§12 (fusion — incl. quorum — requires a total independent/dependent declaration)"

[[test]]
id = "gov_attester_unverified_accepted"
section = "13_governance"
path = "tests/13_governance/gov_attester_unverified_accepted.ag"
expect = "accept"
contains = ["PendingPrincipalDecision", "PrincipalDecision", "Endorsed", "Decided"]
manifest = ["security.attesters.alice.driver=none"]
principal = "grant"
spec = "§13, §17.1 (the default `none` attester authenticator is ACCEPTED, not rejected: the ruling's attester is taken on trust and recorded — marked unverified in the PrincipalDecision attestation — so a fresh local-dev gate still defers, rules, and resumes without a bound authenticator)"

[[test]]
id = "gov_attester_verified_resumes"
section = "13_governance"
path = "tests/13_governance/gov_attester_verified_resumes.ag"
expect = "accept"
contains = ["PendingPrincipalDecision", "PrincipalDecision", "Endorsed", "Decided"]
manifest = ["security.attesters.alice.driver=host"]
principal = "grant"
attester = "alice"
spec = "§13, §17.7 (a host authenticator is bound; the attested ruling's verified identity resolves to the deferred principal, so the attester-match passes, PrincipalDecision is recorded, and the reaction resumes through endorse to the sink)"

[[test]]
id = "gov_attester_wrong_principal_rejected"
section = "13_governance"
path = "tests/13_governance/gov_attester_wrong_principal_rejected.ag"
expect = "accept"
contains = ["PendingPrincipalDecision", "FailedPrincipalDecision", "Decided"]
absent = ["PrincipalDecision", "Endorsed"]
manifest = ["security.attesters.alice.driver=host"]
principal = "grant"
attester = "mallory"
spec = "§13, §16.4 (a host authenticator is bound; the attested ruling's verified identity resolves to a DIFFERENT principal than the gate deferred to, so the attester-match rejects the ruling — FailedPrincipalDecision, the decision stays abstained, fail-closed, and no subject reaches the sink)"

[[test]]
id = "gov_bare_decision_no_perform_reject"
section = "13_governance"
path = "tests/13_governance/gov_bare_decision_no_perform_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§13 (a sealed Decision<E> alone does not settle a subject; performing the raw artifact without an `endorse` is a taint violation)"

[[test]]
id = "gov_conformal_coldstart_abstains"
section = "13_governance"
path = "tests/13_governance/gov_conformal_coldstart_abstains.ag"
expect = "accept"
contains = ["Decided"]
absent = ["Endorsed"]
spec = "§13 (a conformal gate with no recorded decisions is below its labelled-case readiness floor and records a Decided abstention — the supervised cold start)"

[[test]]
id = "gov_conformal_gate_ok"
section = "13_governance"
path = "tests/13_governance/gov_conformal_gate_ok.ag"
expect = "accept"
spec = "§13 (the conformal basis `by conformal α` is a distribution-free finite-sample gate calibrated from the ledger)"

[[test]]
id = "gov_consequential_bare_collapse_reject"
section = "13_governance"
path = "tests/13_governance/gov_consequential_bare_collapse_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§13 (a sealed Decision may guide control flow but is not a subject endorsement → it may not license a perform)"
note = "only a recorded subject endorsement (endorse) may license a consequential action."

[[test]]
id = "gov_endorse_abstain_ok"
section = "13_governance"
path = "tests/13_governance/gov_endorse_abstain_ok.ag"
expect = "accept"
spec = "§13 (the else branch runs when the gate abstains; no Endorsement is constructed for abstinence)"

[[test]]
id = "gov_endorse_abstain_sink_reject"
section = "13_governance"
path = "tests/13_governance/gov_endorse_abstain_sink_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§13, §15.3.3 (abstinence is a Decision outcome, not an Endorsement; endorse requires a committed-narrowed Decision)"

[[test]]
id = "gov_endorse_artifact_allows_perform"
section = "13_governance"
path = "tests/13_governance/gov_endorse_artifact_allows_perform.ag"
expect = "accept"
contains = ["Endorsed"]
spec = "§13 (a generated artifact is settled by endorsing it; the endorsement is sink-admissible only in a committed branch)"

[[test]]
id = "gov_endorse_records_endorsed_ok"
section = "13_governance"
path = "tests/13_governance/gov_endorse_records_endorsed_ok.ag"
expect = "accept"
contains = ["Decided", "Endorsed"]
spec = "§9, §13 (decide records Decided, and endorse records an Endorsed event tied to that decision_id)"

[[test]]
id = "gov_endorse_subject_scope_reject"
section = "13_governance"
path = "tests/13_governance/gov_endorse_subject_scope_reject.ag"
expect = "reject"
error = "GateError"
spec = "§13 (an endorsed subject must be in the decision's dependency scope; a decision about one subject cannot endorse another)"

[[test]]
id = "gov_endorsed_perform_ok"
section = "13_governance"
path = "tests/13_governance/gov_endorsed_perform_ok.ag"
expect = "accept"
contains = ["Endorsed"]
spec = "§13 (a recorded subject endorsement licenses a consequential perform in its committed branch)"

[[test]]
id = "gov_endorsed_subject_allows_perform"
section = "13_governance"
path = "tests/13_governance/gov_endorsed_subject_allows_perform.ag"
expect = "accept"
contains = ["Endorsed"]
spec = "§13 (endorsing the exact subject settles it; the endorsement is sink-admissible inside a committed branch, licensing a perform)"

[[test]]
id = "gov_endorsement_subject_collision_accept"
section = "13_governance"
path = "tests/13_governance/gov_endorsement_subject_collision_accept.ag"
expect = "accept"
spec = "§13 (Endorsement metadata accessors win name collisions; the subject field remains reachable through `.subject`)"

[[test]]
id = "gov_grants_star_ok"
section = "13_governance"
path = "tests/13_governance/gov_grants_star_ok.ag"
expect = "accept"
contains = ["Endorsed"]
spec = "§13 (grants { * } is the explicit unconstrained opt-out — lattice top)"

[[test]]
id = "gov_margin_floor_abstains"
section = "13_governance"
path = "tests/13_governance/gov_margin_floor_abstains.ag"
expect = "accept"
contains = ["Decided"]
absent = ["Endorsed"]
provider = "credence(true=0.55, false=0.45)"
spec = "§13 (a rule's `margin δ` requires the top-vs-runner-up lead ≥ δ at decision time; a 0.10 lead below the 0.20 margin records a Decided abstention, so no Endorsed is recorded)"

[[test]]
id = "gov_pending_principal_decision_records"
section = "13_governance"
path = "tests/13_governance/gov_pending_principal_decision_records.ag"
expect = "accept"
contains = ["PendingPrincipalDecision", "PrincipalDecision", "Decided", "Endorsed"]
order = ["PendingPrincipalDecision", "PrincipalDecision", "Decided"]
principal = "grant"
spec = "§13 (a principal-prefixed `p decide c by r` that cannot commit DEFERS: it appends a durable PendingPrincipalDecision receipt before the attested ruling resolves it to PrincipalDecision, then the canonical Decided)"

[[test]]
id = "gov_perform_reach_subtractive_reject"
section = "13_governance"
path = "tests/13_governance/gov_perform_reach_subtractive_reject.ag"
expect = "reject"
error = "AuthorityViolation"
spec = "§5, §13 (grants are subtractive under extend for `perform`/`reach` too — a child may not exceed its parent's authority)"
note = "Base grants nothing; Child claims `perform Flag`, which Base does not hold"

[[test]]
id = "gov_perform_ungranted_reject"
section = "13_governance"
path = "tests/13_governance/gov_perform_ungranted_reject.ag"
expect = "reject"
error = "AuthorityViolation"
spec = "§13 (default-deny: an agent may only perform actions in its grants)"

[[test]]
id = "gov_principal_decision_deny_fails"
section = "13_governance"
path = "tests/13_governance/gov_principal_decision_deny_fails.ag"
expect = "accept"
contains = ["FailedPrincipalDecision", "Decided"]
absent = ["Endorsed"]
principal = "deny"
spec = "§13 (when the principal declines, `p decide c by r` records FailedPrincipalDecision and a Decided abstention)"

[[test]]
id = "gov_principal_decision_ok"
section = "13_governance"
path = "tests/13_governance/gov_principal_decision_ok.ag"
expect = "accept"
contains = ["PrincipalDecision", "Decided", "Endorsed"]
principal = "grant"
spec = "§13 (`p decide c by r` reaches identity when the rule cannot commit, records PrincipalDecision, records Decided, then may endorse the subject)"

[[test]]
id = "gov_principal_decision_records"
section = "13_governance"
path = "tests/13_governance/gov_principal_decision_records.ag"
expect = "accept"
contains = ["PrincipalDecision", "Decided", "Endorsed"]
principal = "grant"
spec = "§13, §16.4 (a granted `p decide c by r` records PrincipalDecision, then the canonical Decided outcome)"

[[test]]
id = "gov_raw_subject_to_sink_reject"
section = "13_governance"
path = "tests/13_governance/gov_raw_subject_to_sink_reject.ag"
expect = "reject"
error = "TaintViolation"
spec = "§13 (endorse never settles the raw subject variable; only the endorsement binder reaches a sink — performing the raw subject is rejected, in any branch)"

[[test]]
id = "gov_reach_ungranted_reject"
section = "13_governance"
path = "tests/13_governance/gov_reach_ungranted_reject.ag"
expect = "reject"
error = "AuthorityViolation"
spec = "§13 (sending into another agent requires a `reach` grant)"

[[test]]
id = "repro_chain_head_equal"
section = "15_reproducibility"
path = "tests/15_reproducibility/repro_chain_head_equal.ag"
expect = "accept"
replay = "chain_head_equal"
spec = "§15.4.2, §15.5 / T4 (a recorded run replays to an identical chain-head: every oracle result is re-served from the journal in order and nothing is re-invoked)"

[[test]]
id = "repro_decision_records_decided"
section = "15_reproducibility"
path = "tests/15_reproducibility/repro_decision_records_decided.ag"
expect = "accept"
contains = ["Decided"]
spec = "§13, §15 (`decide c by R` records a Decided ledger event while remaining pure/no dependency reach)"

[[test]]
id = "cfg_manifest_decision_policy_reject"
section = "16_config"
path = "tests/16_config/cfg_manifest_decision_policy_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["policy.threshold=0.5"]
spec = "§17.1, §17.2 (decision rules live in source, not the manifest; config cannot set gate thresholds)"

[[test]]
id = "cfg_memory_durable_local_reject"
section = "16_config"
path = "tests/16_config/cfg_memory_durable_local_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["memory.driver=local"]
spec = "§10, §16.7, §17.1 (local/mock advertises session retention only and rejects a durable descriptor before execution)"

[[test]]
id = "cfg_memory_markdown_mixed_retention_accept"
section = "16_config"
path = "tests/16_config/cfg_memory_markdown_mixed_retention_accept.ag"
expect = "accept"
manifest = ["memory.driver=markdown"]
spec = "§10, §16.7, §17.1 (markdown advertises both durable exact storage and an in-process session tier)"

[[test]]
id = "cfg_missing_principal_binding_reject"
section = "16_config"
path = "tests/16_config/cfg_missing_principal_binding_reject.ag"
expect = "reject"
error = "ConfigError"
spec = "§17.1 (each principal declaration resolves to an identity binding; an unbound declared dependency is ALWAYS a ConfigError, not a late runtime lookup — no opt-in flag)"

[[test]]
id = "cfg_missing_prompt_binding_reject"
section = "16_config"
path = "tests/16_config/cfg_missing_prompt_binding_reject.ag"
expect = "reject"
error = "ConfigError"
spec = "§17.1 (each prompt declaration resolves to a manifest binding; an unbound declared dependency is ALWAYS a ConfigError, not a late runtime lookup — no opt-in flag)"

[[test]]
id = "cfg_no_implicit_internalization"
section = "16_config"
path = "tests/16_config/cfg_no_implicit_internalization.ag"
expect = "accept"
absent = ["Internalized"]
spec = "§10, §16.7 (an agent send does not implicitly consult or write private memory)"
note = "source or an explicit host memory operation must name every consultation and mutation"

[[test]]
id = "cfg_reply_memory_is_explicit"
section = "16_config"
path = "tests/16_config/cfg_reply_memory_is_explicit.ag"
expect = "accept"
absent = ["Internalized"]
spec = "§10, §16.7 (memory mutation is a source-authored operation on a declared mem region)"
note = "a provider reply resolves the typed send but does not mutate private memory without an explicit mem store"

[[test]]
id = "cfg_require_fallback_temperature_reject"
section = "16_config"
path = "tests/16_config/cfg_require_fallback_temperature_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["provider.exposes_logprobs=false", "provider.temperature=0"]
spec = "§17 (a text-only provider at temperature 0 requires fallback_temperature for the sampling fallback)"

[[test]]
id = "cfg_sampling_fallback"
section = "16_config"
path = "tests/16_config/cfg_sampling_fallback.ag"
expect = "accept"
contains = ["Endorsed"]
manifest = ["provider.exposes_logprobs=false", "provider.fallback_samples=10", "provider.fallback_temperature=0.7"]
provider = "credence(true=0.9, false=0.1)"
spec = "§16.8, §17 (a text-only provider is served by the sampling fallback: the credence is the empirical frequency of N forced draws; a confident judgment still commits)"
note = "the manifest sets only connector config; the decision rule is in source"

[[test]]
id = "cfg_sampling_fallback_disabled_defers"
section = "16_config"
path = "tests/16_config/cfg_sampling_fallback_disabled_defers.ag"
expect = "accept"
contains = ["Decided"]
absent = ["Endorsed"]
manifest = ["provider.exposes_logprobs=false", "provider.sampling_fallback=false"]
spec = "§13, §17 (without logprobs or the sampling fallback, a conformal gate has no distribution and degrades to deferral/abstain)"

[[test]]
id = "cfg_strict_bindings_ok"
section = "16_config"
path = "tests/16_config/cfg_strict_bindings_ok.ag"
expect = "accept"
manifest = ["identity.alice.driver=local_keyring", "prompts.question.driver=stdin", "tools.search.driver=mock", "actions.Search.tool=search"]
spec = "§17.1 (declared dependencies pass configuration binding when every dependency has a manifest entry and every wiring references an existing catalog key and a declared name)"

[[test]]
id = "cfg_tool_binding_missing_driver_reject"
section = "16_config"
path = "tests/16_config/cfg_tool_binding_missing_driver_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["tools.search.provider=tavily", "actions.Search.tool=search"]
spec = "§17.1 (a [tools.*] catalog entry must name a driver; connector-specific fields are not enough)"

[[test]]
id = "cfg_tool_host_binding_accept"
section = "16_config"
path = "tests/16_config/cfg_tool_host_binding_accept.ag"
expect = "accept"
manifest = ["tools.search.driver=host", "tools.search.adapter=localSearch", "actions.Search.tool=search"]
spec = "§6b, §17.1 (a [tools.*] catalog entry can bind to implementation-defined host functions, scripts, processes, or skills; the wiring is unchanged)"

[[test]]
id = "cfg_tool_mcp_binding_accept"
section = "16_config"
path = "tests/16_config/cfg_tool_mcp_binding_accept.ag"
expect = "accept"
manifest = ["tools.search.driver=mcp", "tools.search.server=stdio:local-search", "actions.Search.tool=search"]
spec = "§6b, §17.1 ([tools.*] is the endpoint catalog and MCP is one supported transport; an action wires to a catalog entry by its key)"

[[test]]
id = "cfg_wiring_missing_catalog_key_reject"
section = "16_config"
path = "tests/16_config/cfg_wiring_missing_catalog_key_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["actions.Search.tool=search"]
spec = "§6b, §17.1 (an [actions.NAME] wiring must reference an existing [tools.*] catalog entry; a missing catalog key is ALWAYS a ConfigError, not a late runtime lookup)"

[[test]]
id = "cfg_wiring_undeclared_action_reject"
section = "16_config"
path = "tests/16_config/cfg_wiring_undeclared_action_reject.ag"
expect = "reject"
error = "ConfigError"
manifest = ["tools.search.driver=mock", "actions.Search.tool=search"]
spec = "§6b, §17.1 (an [actions.NAME]/[events.NAME] wiring must name a DECLARED action or event; wiring an undeclared name is a ConfigError)"

[[test]]
id = "gate_cold_consequential_defers_to_principal"
section = "22_gate"
path = "tests/22_gate/gate_cold_consequential_defers_to_principal.ag"
expect = "accept"
contains = ["PrincipalDecision", "Endorsed"]
provider = "credence(Approve=0.9, Reject=0.1)"
principal = "grant"
spec = "§13 (a cold consequential path reaches a principal before the action commits; the principal's ruling is the first label)"

[[test]]
id = "gate_consequential_no_principal_reject"
section = "22_gate"
path = "tests/22_gate/gate_consequential_no_principal_reject.ag"
expect = "reject"
error = "GateError"
spec = "§13 (a consequential endorsement path with no principal escalation and no mature profile is a compile error — autonomy is earned, deference is required)"

[[test]]
id = "gate_decide_principal_subject_accept"
section = "22_gate"
path = "tests/22_gate/gate_decide_principal_subject_accept.ag"
expect = "accept"
contains = ["PrincipalDecision", "Endorsed"]
principal = "grant"
spec = "§13 (`p decide c by r` uses a principal escalation prefix; the endorsed subject reaches the sink in the committed branch)"

[[test]]
id = "gate_decision_field_readonly_reject"
section = "22_gate"
path = "tests/22_gate/gate_decision_field_readonly_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§13 (Decision provenance fields are read-only)"

[[test]]
id = "gate_decision_introspection_accept"
section = "22_gate"
path = "tests/22_gate/gate_decision_introspection_accept.ag"
expect = "accept"
spec = "§13, §9 (Decision provenance: .basis over the Basis enum)"

[[test]]
id = "gate_decision_introspection_committed_margin_accept"
section = "22_gate"
path = "tests/22_gate/gate_decision_introspection_committed_margin_accept.ag"
expect = "accept"
spec = "§13 (Decision provenance exposes .committed and .margin, not only .basis)"

[[test]]
id = "gate_decision_not_endorsement_reject"
section = "22_gate"
path = "tests/22_gate/gate_decision_not_endorsement_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§13 (`decide c by R` yields a Decision<E>, not an Endorsement<E>)"

[[test]]
id = "gate_deference_in_else_accept"
section = "22_gate"
path = "tests/22_gate/gate_deference_in_else_accept.ag"
expect = "accept"
contains = ["PrincipalDecision", "Decided", "Endorsed"]
principal = "grant"
spec = "§13 (deference is an ordinary principal-prefixed `decide` in the else branch when the autonomous gate abstains)"

[[test]]
id = "gate_endorsement_expr_type_accept"
section = "22_gate"
path = "tests/22_gate/gate_endorsement_expr_type_accept.ag"
expect = "accept"
contains = ["Endorsed"]
spec = "§13 (endorse yields an Endorsement<T>, the recorded settled form of a gate decision)"

[[test]]
id = "gate_endorsement_field_readonly_reject"
section = "22_gate"
path = "tests/22_gate/gate_endorsement_field_readonly_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§13 (Endorsement provenance fields are read-only)"

[[test]]
id = "gate_endorsement_introspection_accept"
section = "22_gate"
path = "tests/22_gate/gate_endorsement_introspection_accept.ag"
expect = "accept"
spec = "§13 (Endorsement exposes the same read-only provenance fields as Decision)"

[[test]]
id = "gate_endorsement_not_decision_reject"
section = "22_gate"
path = "tests/22_gate/gate_endorsement_not_decision_reject.ag"
expect = "reject"
error = "TypeError"
spec = "§13 (an Endorsement<E> is not a Decision<E>)"

[[test]]
id = "gate_file_conformal_decl_accept"
section = "22_gate"
path = "tests/22_gate/gate_file_conformal_decl_accept.ag"
expect = "accept"
contains = ["Endorsed"]
principal = "grant"
spec = "§13, §15.2 (a file-level `conformal α;` declaration sets the default conformal level)"

[[test]]
id = "gate_per_gate_conformal_accept"
section = "22_gate"
path = "tests/22_gate/gate_per_gate_conformal_accept.ag"
expect = "accept"
contains = ["Endorsed"]
principal = "grant"
spec = "§13 (a per-gate conformal α on a decide expression, distinct from the file default)"

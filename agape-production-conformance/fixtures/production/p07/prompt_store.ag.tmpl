prompt text question;

agent Rememberer {
  mem notes {
    type text;
    modality opaque;
    scope project;
    retention durable;
  }
  when (Prompt p about question) {
    notes <- f"${p.text}";
  }
}

spawn Rememberer remembered_agent;
awake remembered_agent;
